// component for installing metamask

const Install = () => {
    return (
      <div>
        <h3>To connect with the blochain, install MetaMask </h3>
        <a href="https://metamask.io/download.html">Meta Mask</a>
      </div>
    );
  };
  
export default Install;